#!/bin/bash

# interface=`ifconfig | head -1 | cut -d: -f1`

# `ifconfig $interface 10.0.0.15`
# `ifconfig $interface netmask 255.255.255.0`

# username=$1
# password=$2
# group=$3
# useradd -g $group $username
# # echo "$username:$password" | chpasswd 
# echo -e "$password\n$password" | passwd $username
# echo -e "$password\n$password" | smbpasswd -as  $username








